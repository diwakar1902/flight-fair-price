# Server-side only — no VITE_ prefix, so Vite never bundles this into client code.
TRAVELPAYOUTS_TOKEN=
# Primary live-price source (100 free searches/month). Falls back to
# Travelpayouts cache automatically when unset, exhausted, or erroring.
SERPAPI_KEY=
